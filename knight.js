export class Knight {
    constructor(row, col) {
        this.posX = row;
        this.posY = col;
    }

    travel(startCoords, endCoords, path = []) {
        if (startCoords == endCoords) {
            return startCoords;
        }
    }

    
}